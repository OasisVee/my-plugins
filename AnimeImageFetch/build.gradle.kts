version = "1.0.0"
description = "Get anime images/gifs from waifu.pics API (includes NSFW options)"

aliucord.changelog.set(
        """
            # 1.0.0
            * Initial release with waifu.pics API
            * Added clear NSFW tags for content categories
            * Separated SFW and NSFW endpoints
            * Added NSFW content warnings in embeds and messages
            * Expanded category selection with more options
        """.trimIndent()
)